<!DOCTYPE html>
<html>
<head>
  <title>Workspace</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>
<body>
  <div class="container">
    <ul class="nav nav-tabs">
      <li><%= link_to "Home", welcome_index_path %></li>
        <% if !current_user %>
         <div class= "pull-right">
           <%= link_to "Sign In", new_user_session_path, class: 'navbar-link ' %> or
           <%= link_to "Sign Up", new_user_registration_path, class: 'btn btn-primary navbar-link'%> today!
         </div>
      <% else %>
        <p>
         <%= link_to "Sign Out", destroy_user_session_path, method: :delete, class: 'navbar-link pull-right' %> 
        </p>
         <% end %>
    </ul>
    <p class="notice"><%= notice %></p>
    <p class="alert"><%= alert %></p>
    <%= yield %>
  </div>
</body>
</html>
